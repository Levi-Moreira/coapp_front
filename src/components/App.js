import React  from 'react';
import {Link} from 'react-router';
import LoginPage from '../components/LoginPage'

const App = (props) => {
  return (
    <div>
      <LoginPage/>
    </div>
  );
};

export default App
